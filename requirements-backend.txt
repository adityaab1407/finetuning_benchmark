# Backend (FastAPI) dependencies — excludes heavy training libs (torch, etc.)
groq>=0.11
pydantic>=2.0
pydantic-settings>=2.0
python-dotenv>=1.0

# Data ingestion & processing
datasets>=2.18
huggingface-hub>=0.23
requests>=2.31
beautifulsoup4>=4.12
lxml>=5.1

# Vector DB + RAG
chromadb>=0.5

# Evaluation & statistics
scipy>=1.12
numpy>=1.26
rapidfuzz>=3.6

# Serving
fastapi>=0.111
uvicorn[standard]>=0.29
httpx>=0.27

# Observability (optional — won't fail if keys absent)
langfuse>=2.27
wandb>=0.16
